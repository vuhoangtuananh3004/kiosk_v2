/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['images.pexels.com','mdbcdn.b-cdn.net', 'tcdn.tchpt.com'],
  },
}

module.exports = nextConfig
